create table IF_DTD_004_S
(
    ESB_SEQ            NUMBER primary key,
    ESB_STATUS_CD      VARCHAR2(1) DEFAULT 'N' NOT NULL,
    ESB_TRANSACTION_ID VARCHAR2(40),
    ESB_RECV_DT        VARCHAR2(20),
    ESB_INTERFACE_ID   VARCHAR2(40),
    CAR_MODEL          VARCHAR(50),
    CAR_YEAR           VARCHAR(50),
    CAR_BRAND          VARCHAR(50),
    CAR_VIN            VARCHAR(50)
);

CREATE TABLE IF_DTD_004_S_CHILD
(
    CAR_SEQ   NUMBER primary key,
    CAR_MODEL VARCHAR2(50),
    AUDIO     VARCHAR2(255),
    COLOR     VARCHAR2(50),
    TIRES     VARCHAR2(50)
);

create table IF_DTD_004_T
(
    ESB_SEQ            NUMBER primary key,
    ESB_STATUS_CD      VARCHAR2(1) DEFAULT 'N' NOT NULL,
    ESB_TRANSACTION_ID VARCHAR2(40),
    ESB_RECV_DT        VARCHAR2(20),
    ESB_INTERFACE_ID   VARCHAR2(40),
    CAR_MODEL          VARCHAR(50),
    CAR_YEAR           VARCHAR(50),
    CAR_BRAND          VARCHAR(50),
    CAR_VIN            VARCHAR(50)
);

CREATE TABLE IF_DTD_004_T_CHILD
(
    CAR_SEQ   NUMBER primary key,
    CAR_MODEL VARCHAR2(50),
    AUDIO     VARCHAR2(255),
    COLOR     VARCHAR2(50),
    TIRES     VARCHAR2(50)
);

CREATE OR REPLACE PROCEDURE SP_IF_DTD_004_T(txid IN VARCHAR2)
    IS
BEGIN
    DBMS_OUTPUT.PUT_LINE(txid || ' test');
    UPDATE IF_DTD_004_T SET ESB_STATUS_CD = 'S' WHERE ESB_TRANSACTION_ID = txid;
END;

CREATE SEQUENCE SEQ_IF_DTD_004_T INCREMENT BY 1 MINVALUE 1 MAXVALUE 9999999999 NOCYCLE NOCACHE NOORDER;